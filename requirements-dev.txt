# Resolved development dependencies, pinned so that a new release of a tool
# cannot change what runs on a given commit. The declared ranges live in
# pyproject.toml under [project.optional-dependencies] dev; this file is one
# resolution of those ranges, and the one CI installs.
#
# Runtime dependencies are deliberately absent: the tools have none. Nothing
# here ships to a user — it is the test and lint toolchain only.
#
# Regenerate after changing the dev extras:
#   python3 -m venv .venv-lock
#   .venv-lock/bin/pip install -e ".[dev]"
#   .venv-lock/bin/pip freeze --exclude-editable > requirements-dev.txt
# then restore this header.

cfgv==3.5.0
coverage[toml]==7.15.4
distlib==0.4.3
filelock==3.32.3
identify==2.6.19
iniconfig==2.3.0
nodeenv==1.10.0
packaging==26.3
platformdirs==4.11.3
pluggy==1.6.0
pre_commit==4.6.2
Pygments==2.20.0
pytest==9.1.1
pytest-cov==7.1.0
python-discovery==1.5.2
PyYAML==6.0.3
ruff==0.16.3
virtualenv==21.7.4
